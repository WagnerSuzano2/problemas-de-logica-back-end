let distancia = 500; // distacia em metros
let tempo = 10; // tempo em segundos

const velocidade = (distancia / tempo) * 3.6; // velocidade em km/h

console.log(`A velocidade é de ${velocidade.toFixed(2)} km/h`); // exibe a velocidade
